video_input_path: "data/input_video.mp4"
video_output_path: "data/output_video.mp4"
yolo_pose_model_path: "models/yolov8n-pose.pt"
tracknet_model_path: "models/tracknetv2_weights.pth"

# --- 基于日志分析的优化参数 ---
pose_confidence_threshold: 0.5
ball_confidence_threshold: 0.6
static_ball_movement_threshold_px: 5
static_ball_frames_threshold: 7

# Swing classification parameters
forehand_wrist_elbow_angle_min: 90
forehand_wrist_elbow_angle_max: 170
backhand_wrist_elbow_angle_min: 90
backhand_wrist_elbow_angle_max: 170
two_hand_wrist_distance_max_px: 50
dominant_hand: "right"

# Ball tracking and filtering parameters
ball_trajectory_max_len: 30
max_lost_frames_for_track: 5
max_ball_match_distance_px: 25
static_ball_movement_threshold_px: 5
static_ball_frames_threshold: 8

# 📊 根据日志分析优化的球尺寸过滤参数
min_ball_radius: 3
max_ball_radius: 20                     # 从15扩大到20px
min_ball_movement: 6                    # 从8降低到6

# 🎯 根据日志分析优化的边界设置
use_boundary: true
boundary_x1: 400                    # 大幅扩大左边界 (650→400)
boundary_y1: 300                    # 大幅扩大上边界 (410→300)
boundary_x2: 1500                   # 扩大右边界 (1420→1500)
boundary_y2: 1000                   # 保持下边界
draw_boundary: true

# 显示控制
draw_static_balls: false

# Simulation specific parameters
sim_ball_detection_noise: 3
sim_num_static_balls: 0

# 字体配置
font_dir: "fonts"
preferred_chinese_font: "PingFang SC"
auto_download_font: false

# Racket Detector Configs
racket_yolo_model_path: "yolov8n.pt"
racket_confidence_threshold: 0.4
racket_lost_threshold_frames: 5
ball_radius_px: 10

enable_manual_boundary: False

# 视频文本显示控制
display_options:
  show_swing_type: true
  show_ball_position: true
  show_racket_state: true
  show_pose_keypoints: true
  show_ball_trajectory: true
  show_boundary: true
  show_static_balls: false
  show_fps: true
  show_frame_number: false

# 高级人脸检测器配置
advanced_face_detection:
  detection_priority: ["enhanced_opencv"]
  
  detector_control:
    enable_front_face: true
    enable_front_face_alt: false
    enable_front_face_alt2: false
    enable_profile_face: false
    enable_full_body: false
  
  opencv_detection_params:
    front_face:
      scale_factor: 1.3
      min_neighbors: 2
      confidence: 0.7
  
  face_size_constraints:
    min_face_size: 40
    max_face_size: 120
    min_aspect_ratio: 0.6
    max_aspect_ratio: 1.8
    relative_max_size: 0.3
  
  quality_assessment:
    contrast_normalization_factor: 60.0
    target_brightness: 128
    brightness_tolerance: 100
    canny_low_threshold: 60
    canny_high_threshold: 180
    edge_density_factor: 8.0
    size_normalization_factor: 8000.0
  
  quality_filters:
    min_confidence: 0.7
    min_quality_score: 0.3
    enable_opencv_quality_filter: false
    max_detections_per_frame: 1
    enable_nms: true
    nms_iou_threshold: 0.5
  
  detection_merging:
    overlap_threshold: 0.4
    min_overlap_for_merge: 0.4
  
  face_tracking:
    enable_tracking: true
    max_disappeared_frames: 15
    max_match_distance: 80
    smoothing_factor: 0.8
    position_smooth_alpha: 0.8
  
  position_constraints:
    exclude_edges: false
    edge_margin_ratio: 0.05
    center_bias_weight: 0.0
    max_distance_from_center: 1.0
    center_preference: false
    center_weight: 1.0
    exclude_logo_areas: false
    logo_exclusion_zones: []
  
  mtcnn:
    enabled: false

# GLIP模型配置
glip_model:
  enabled: false

# 头像替换配置
face_replacement:
  enabled: false
  judy_head_image_path: "assets/judy_head.png"
  judy_head_scale_factor: 0.8
  blend_mode: "alpha"
  face_detection_method: "opencv"
  replacement_opacity: 0.95
  face_margin_ratio: 0.05
  smooth_transition: true
  track_faces: true

# 调试选项
debug_mode: false

# 🔍 球检测详细日志配置
ball_detection_debug:
  enabled: true
  log_hsv_detection: true
  log_boundary_check: true
  log_size_filtering: true
  log_static_filtering: false
  log_advanced_processing: false
  log_simulation: false
  save_debug_frames: true
  debug_frame_interval: 2
  max_log_detections: 15 