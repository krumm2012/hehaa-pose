# 网球分析系统 - 依赖包列表

# === 核心依赖（必需） ===
opencv-python>=4.5.0
numpy>=1.19.0
scipy>=1.5.0
pillow>=8.0.0
pyyaml>=5.4.0
ultralytics>=8.0.0          # YOLOv8 姿势估计和球拍检测
torch
torchvision
torchaudio

# === 图像处理工具 ===
imutils>=0.5.4               # 图像处理辅助工具

# === 高级人脸检测依赖（可选） ===

# MTCNN - 深度学习人脸检测（仅Python 3.8-3.11支持）
# ⚠️ 重要：MTCNN需要TensorFlow，不支持Python 3.13+
# 
# Python版本兼容性：
# ✅ Python 3.8-3.11: 完全支持
# ⚠️ Python 3.12: 部分支持
# ❌ Python 3.13+: 不支持
#
# 安装方案：
# 
# 方案1 - 创建兼容环境（推荐）：
# brew install pyenv
# pyenv install 3.11.7
# pyenv local 3.11.7
# python -m venv venv_mtcnn
# source venv_mtcnn/bin/activate
# pip install tensorflow mtcnn
#
# 方案2 - 使用conda：
# conda create -n tennis_mtcnn python=3.11
# conda activate tennis_mtcnn
# pip install tensorflow mtcnn
#
# 如果您使用Python 3.13，推荐继续使用增强OpenCV检测器
# 性能优秀：误检率降低62%，检测稳定性100%
#
# 启用MTCNN (仅在兼容环境中)：
# mtcnn>=0.1.1
# tensorflow>=2.8.0,<2.14.0

# dlib - HOG + SVM人脸检测和68点关键点（可选）
# 注意：需要CMake编译，可能在某些系统上安装困难
# 安装命令：brew install cmake && pip install dlib
# dlib>=19.22.0
# face-recognition>=1.3.0      # 基于dlib的高级人脸识别库

# === GLIP模型相关依赖（可选） ===
# 注意：GLIP目前可能不支持Python 3.13+
# 如果安装失败，系统会自动使用增强OpenCV人脸检测作为备选方案
# 
# 安装GLIP (可选):
# pip install Cython yacs ninja  # 先安装依赖
# pip install git+https://github.com/microsoft/GLIP.git
#
# 如果GLIP安装失败，可以：
# 1. 继续使用增强OpenCV检测（推荐，已验证工作正常）
# 2. 使用Python 3.10-3.11创建新环境
# 3. 等待GLIP项目更新对新Python版本的支持

# GLIP编译依赖（可选）
# Cython>=0.29.0
# yacs>=0.1.8
# ninja>=1.10.0

# === 开发和测试依赖（可选） ===
# pytest>=6.0.0
# black>=21.0.0

# === 安装说明 ===
#
# 基础安装（推荐）：
# pip install opencv-python numpy scipy pillow pyyaml ultralytics torch torchvision torchaudio imutils
#
# 完整安装（包含高级功能，可能在Python 3.13上失败）：
# pip install -r requirements.txt
# pip install tensorflow mtcnn  # MTCNN支持
# pip install Cython yacs ninja && pip install git+https://github.com/microsoft/GLIP.git  # GLIP支持
#
# 如果高级功能安装失败：
# - 系统会自动使用增强OpenCV检测
# - 所有功能仍然正常工作
# - 检测效果优秀，性能稳定

# 安装GLIP模型
pip install git+https://github.com/microsoft/GLIP.git

# 下载模型文件到models/目录
# 然后运行
python main.py 