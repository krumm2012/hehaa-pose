# 2026-08-01 工作日志

## 项目代码库分析与总结
- 对 tennis_analyzer 全库（152 个 .py、55 个测试文件）做了完整分析，生成 `PROJECT_SUMMARY.md`（项目根目录）。
- 项目定位：本地 AI 网球视频分析系统，单机位提取姿态/球/拍/挥拍事件，输出面向 AI 教练的结构化数据。
- 关键事实（长期有效）：
  - 主分支 `new`，远端 `origin-paused`，HEAD c645ec5（2026-07-29 实时分析流水线版本化隔离）。
  - 技术栈：YOLO26 系列（yolo26n 统一检测 + yolo26m-pose），Core ML .mlpackage + ANE，多进程 shared_memory 流水线（main_pipe.py 1572 行），DeepSeek 异步旁路教练。
  - 架构六层：输入(视频/RTSP/ROI) → 检测(统一 yolo26n+pose) → 处理(静止球抑制/轨迹) → 分析(挥拍事件/生物力学/Coach 数据) → 教练增强(本地规则+DeepSeek+证据门控) → 输出(标注视频/JSON/HTML/评估)。
  - 配置主文件：`configs/yolo26_tennis_config.yaml`；ROI 三场地绑定：`configs/roi_config.yaml`。
  - 已知瓶颈：挥拍类型准确率仅 50%（03.15 人工标注评估），contact frame 100%；缺大规模人工真值标注集。
  - 环境坑：存在 4 套 venv（venv/venv_311/venv_tracknet/venv_yolo26），运行主流水线用 `venv_yolo26/bin/python`。
  - 本地控制台：`local_control_panel.py --open` → http://127.0.0.1:8765/，RTSP 凭据走环境变量 TENNIS_RTSP_USERNAME/PASSWORD。
